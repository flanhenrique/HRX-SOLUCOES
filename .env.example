# Public website
VITE_HRX_QUOTE_ENDPOINT=

# Admin preview is OFF by default. Never enable it in production without auth/backend.
VITE_HRX_ADMIN_UI=false

# Future authenticated admin integration
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_HRX_ADMIN_ENDPOINT=

# Supabase Edge Function secrets are configured in Supabase, never in Vite/GitHub:
# HRX_ALLOWED_ORIGINS=https://seu-dominio.example,http://localhost:5173
# SUPABASE_URL=managed-by-supabase
# SUPABASE_SERVICE_ROLE_KEY=managed-secret
